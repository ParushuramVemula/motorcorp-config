server:
  port: 8082
spring:
  application:
    name: Inventory-Management

eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      default-zone: http://localhost:9096/eureka